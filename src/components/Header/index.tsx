import React from 'react';

import { Container } from './styles';

const Header: React.FC = () => {
  return (
    <Container>
      <h2>Talk UP</h2>
      <button type="button">
        <div />
      </button>
    </Container>
  );
};

export default Header;
